# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseModel, Field

from . import types, enums, errors, fields, bases
from ._types import FuncType
from ._compat import model_rebuild, field_validator
from ._builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class DrawingAttempt(bases.BaseDrawingAttempt):
    """Represents a DrawingAttempt record"""

    id: _str
    character: _str
    characterType: _str
    userDrawing: _str
    referenceImage: _str
    processedImage: _str
    skeletonImage: Optional[_str] = None
    finalScore: _float
    ssimScore: Optional[_float] = None
    contourScore: Optional[_float] = None
    skeletonScore: Optional[_float] = None
    confidenceScore: Optional[_float] = None
    isCorrect: _bool
    qualityLabel: _str
    userId: Optional[_str] = None
    sessionId: Optional[_str] = None
    deviceInfo: Optional[_str] = None
    drawingTimeMs: Optional[_int] = None
    evaluationTimeMs: Optional[_int] = None
    submittedAt: datetime.datetime
    isValidSubmission: _bool
    validationFlags: List[_str]
    featureVector: Optional['models.FeatureVector'] = None
    feedbackRecord: Optional['models.FeedbackRecord'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )

    @field_validator('validationFlags', pre=True, allow_reuse=True)
    @classmethod
    def _transform_required_list_fields(cls, value: object) -> object:
        # When using raw queries, some databases will return `None` for an array field that has not been set yet.
        #
        # In our case we want to use an empty list instead as that is the internal Prisma behaviour and we want
        # to use the same consistent structure between the core ORM and raw queries. For example, if we updated
        # our type definitions to include `None` for `List` fields then it would be misleading as it will only
        # ever be `None` in raw queries.
        if value is None:
            return []

        return value

    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DrawingAttemptKeys']] = None,
        exclude: Optional[Iterable['types.DrawingAttemptKeys']] = None,
        required: Optional[Iterable['types.DrawingAttemptKeys']] = None,
        optional: Optional[Iterable['types.DrawingAttemptKeys']] = None,
        relations: Optional[Mapping['types.DrawingAttemptRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DrawingAttemptKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DrawingAttempt_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DrawingAttempt_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DrawingAttempt_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DrawingAttempt_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _DrawingAttempt_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _DrawingAttempt_relational_fields:
                        raise errors.UnknownRelationalFieldError('DrawingAttempt', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DrawingAttempt / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DrawingAttempt',
            }
        )
        _created_partial_types.add(name)


class FeatureVector(bases.BaseFeatureVector):
    """Represents a FeatureVector record"""

    id: _str
    drawingAttemptId: _str
    contourArea: _float
    aspectRatio: _float
    boundingBoxWidth: _float
    boundingBoxHeight: _float
    perimeter: _float
    solidity: _float
    extent: _float
    strokeCount: _int
    loopCount: _int
    endpointCount: _int
    intersectionCount: _int
    complexityScore: _float
    symmetryScore: Optional[_float] = None
    featureVector: 'fields.Json'
    extractedAt: datetime.datetime
    drawingAttempt: Optional['models.DrawingAttempt'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FeatureVectorKeys']] = None,
        exclude: Optional[Iterable['types.FeatureVectorKeys']] = None,
        required: Optional[Iterable['types.FeatureVectorKeys']] = None,
        optional: Optional[Iterable['types.FeatureVectorKeys']] = None,
        relations: Optional[Mapping['types.FeatureVectorRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FeatureVectorKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FeatureVector_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FeatureVector_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FeatureVector_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FeatureVector_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FeatureVector_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FeatureVector_relational_fields:
                        raise errors.UnknownRelationalFieldError('FeatureVector', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FeatureVector / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FeatureVector',
            }
        )
        _created_partial_types.add(name)


class FeedbackRecord(bases.BaseFeedbackRecord):
    """Represents a FeedbackRecord record"""

    id: _str
    drawingAttemptId: _str
    overallFeedback: _str
    encouragement: Optional[_str] = None
    missingStrokes: List[_str]
    proportionIssues: List[_str]
    positioningIssues: List[_str]
    topologyIssues: List[_str]
    suggestions: List[_str]
    practiceAreas: List[_str]
    feedbackType: _str
    priority: _str
    generatedAt: datetime.datetime
    drawingAttempt: Optional['models.DrawingAttempt'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )

    @field_validator('missingStrokes', 'proportionIssues', 'positioningIssues', 'topologyIssues', 'suggestions', 'practiceAreas', pre=True, allow_reuse=True)
    @classmethod
    def _transform_required_list_fields(cls, value: object) -> object:
        # When using raw queries, some databases will return `None` for an array field that has not been set yet.
        #
        # In our case we want to use an empty list instead as that is the internal Prisma behaviour and we want
        # to use the same consistent structure between the core ORM and raw queries. For example, if we updated
        # our type definitions to include `None` for `List` fields then it would be misleading as it will only
        # ever be `None` in raw queries.
        if value is None:
            return []

        return value

    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FeedbackRecordKeys']] = None,
        exclude: Optional[Iterable['types.FeedbackRecordKeys']] = None,
        required: Optional[Iterable['types.FeedbackRecordKeys']] = None,
        optional: Optional[Iterable['types.FeedbackRecordKeys']] = None,
        relations: Optional[Mapping['types.FeedbackRecordRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FeedbackRecordKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FeedbackRecord_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FeedbackRecord_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FeedbackRecord_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FeedbackRecord_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FeedbackRecord_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FeedbackRecord_relational_fields:
                        raise errors.UnknownRelationalFieldError('FeedbackRecord', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FeedbackRecord / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FeedbackRecord',
            }
        )
        _created_partial_types.add(name)


class CharacterProfile(bases.BaseCharacterProfile):
    """Represents a CharacterProfile record"""

    id: _str
    character: _str
    characterType: _str
    difficultyLevel: _float
    strokeComplexity: _float
    hasLoops: _bool
    hasCurves: _bool
    scoreAdjustment: _float
    toleranceLevel: _float
    averageScore: Optional[_float] = None
    successRate: Optional[_float] = None
    totalAttempts: _int
    createdAt: datetime.datetime
    updatedAt: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CharacterProfileKeys']] = None,
        exclude: Optional[Iterable['types.CharacterProfileKeys']] = None,
        required: Optional[Iterable['types.CharacterProfileKeys']] = None,
        optional: Optional[Iterable['types.CharacterProfileKeys']] = None,
        relations: Optional[Mapping['types.CharacterProfileRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CharacterProfileKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _CharacterProfile_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _CharacterProfile_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _CharacterProfile_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _CharacterProfile_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "CharacterProfile" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid CharacterProfile / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'CharacterProfile',
            }
        )
        _created_partial_types.add(name)


class PerformanceMetric(bases.BasePerformanceMetric):
    """Represents a PerformanceMetric record"""

    id: _str
    metricType: _str
    component: _str
    value: _float
    unit: _str
    characterType: Optional[_str] = None
    timestamp: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.PerformanceMetricKeys']] = None,
        exclude: Optional[Iterable['types.PerformanceMetricKeys']] = None,
        required: Optional[Iterable['types.PerformanceMetricKeys']] = None,
        optional: Optional[Iterable['types.PerformanceMetricKeys']] = None,
        relations: Optional[Mapping['types.PerformanceMetricRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.PerformanceMetricKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _PerformanceMetric_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _PerformanceMetric_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _PerformanceMetric_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _PerformanceMetric_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "PerformanceMetric" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid PerformanceMetric / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'PerformanceMetric',
            }
        )
        _created_partial_types.add(name)


class AdminAuditLog(bases.BaseAdminAuditLog):
    """Represents a AdminAuditLog record"""

    id: _str
    adminUserId: _str
    action: _str
    targetType: _str
    targetId: Optional[_str] = None
    oldValue: Optional['fields.Json'] = None
    newValue: Optional['fields.Json'] = None
    justification: Optional[_str] = None
    ipAddress: Optional[_str] = None
    userAgent: Optional[_str] = None
    timestamp: datetime.datetime

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.AdminAuditLogKeys']] = None,
        exclude: Optional[Iterable['types.AdminAuditLogKeys']] = None,
        required: Optional[Iterable['types.AdminAuditLogKeys']] = None,
        optional: Optional[Iterable['types.AdminAuditLogKeys']] = None,
        relations: Optional[Mapping['types.AdminAuditLogRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.AdminAuditLogKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _AdminAuditLog_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _AdminAuditLog_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _AdminAuditLog_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _AdminAuditLog_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True


            if relations:
                raise ValueError('Model: "AdminAuditLog" has no relational fields.')
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid AdminAuditLog / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'AdminAuditLog',
            }
        )
        _created_partial_types.add(name)



_DrawingAttempt_relational_fields: Set[str] = {
        'featureVector',
        'feedbackRecord',
    }
_DrawingAttempt_fields: Dict['types.DrawingAttemptKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('character', {
            'name': 'character',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('characterType', {
            'name': 'characterType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userDrawing', {
            'name': 'userDrawing',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('referenceImage', {
            'name': 'referenceImage',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('processedImage', {
            'name': 'processedImage',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('skeletonImage', {
            'name': 'skeletonImage',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('finalScore', {
            'name': 'finalScore',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('ssimScore', {
            'name': 'ssimScore',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('contourScore', {
            'name': 'contourScore',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('skeletonScore', {
            'name': 'skeletonScore',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('confidenceScore', {
            'name': 'confidenceScore',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('isCorrect', {
            'name': 'isCorrect',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('qualityLabel', {
            'name': 'qualityLabel',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userId', {
            'name': 'userId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sessionId', {
            'name': 'sessionId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('deviceInfo', {
            'name': 'deviceInfo',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('drawingTimeMs', {
            'name': 'drawingTimeMs',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('evaluationTimeMs', {
            'name': 'evaluationTimeMs',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('submittedAt', {
            'name': 'submittedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('isValidSubmission', {
            'name': 'isValidSubmission',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('validationFlags', {
            'name': 'validationFlags',
            'is_list': True,
            'optional': False,
            'type': 'List[_str]',
            'is_relational': False,
            'documentation': None,
        }),
        ('featureVector', {
            'name': 'featureVector',
            'is_list': False,
            'optional': True,
            'type': 'models.FeatureVector',
            'is_relational': True,
            'documentation': None,
        }),
        ('feedbackRecord', {
            'name': 'feedbackRecord',
            'is_list': False,
            'optional': True,
            'type': 'models.FeedbackRecord',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_FeatureVector_relational_fields: Set[str] = {
        'drawingAttempt',
    }
_FeatureVector_fields: Dict['types.FeatureVectorKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('drawingAttemptId', {
            'name': 'drawingAttemptId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('contourArea', {
            'name': 'contourArea',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('aspectRatio', {
            'name': 'aspectRatio',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('boundingBoxWidth', {
            'name': 'boundingBoxWidth',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('boundingBoxHeight', {
            'name': 'boundingBoxHeight',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('perimeter', {
            'name': 'perimeter',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('solidity', {
            'name': 'solidity',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('extent', {
            'name': 'extent',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('strokeCount', {
            'name': 'strokeCount',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('loopCount', {
            'name': 'loopCount',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('endpointCount', {
            'name': 'endpointCount',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('intersectionCount', {
            'name': 'intersectionCount',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('complexityScore', {
            'name': 'complexityScore',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('symmetryScore', {
            'name': 'symmetryScore',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('featureVector', {
            'name': 'featureVector',
            'is_list': False,
            'optional': False,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('extractedAt', {
            'name': 'extractedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('drawingAttempt', {
            'name': 'drawingAttempt',
            'is_list': False,
            'optional': True,
            'type': 'models.DrawingAttempt',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_FeedbackRecord_relational_fields: Set[str] = {
        'drawingAttempt',
    }
_FeedbackRecord_fields: Dict['types.FeedbackRecordKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('drawingAttemptId', {
            'name': 'drawingAttemptId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('overallFeedback', {
            'name': 'overallFeedback',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('encouragement', {
            'name': 'encouragement',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('missingStrokes', {
            'name': 'missingStrokes',
            'is_list': True,
            'optional': False,
            'type': 'List[_str]',
            'is_relational': False,
            'documentation': None,
        }),
        ('proportionIssues', {
            'name': 'proportionIssues',
            'is_list': True,
            'optional': False,
            'type': 'List[_str]',
            'is_relational': False,
            'documentation': None,
        }),
        ('positioningIssues', {
            'name': 'positioningIssues',
            'is_list': True,
            'optional': False,
            'type': 'List[_str]',
            'is_relational': False,
            'documentation': None,
        }),
        ('topologyIssues', {
            'name': 'topologyIssues',
            'is_list': True,
            'optional': False,
            'type': 'List[_str]',
            'is_relational': False,
            'documentation': None,
        }),
        ('suggestions', {
            'name': 'suggestions',
            'is_list': True,
            'optional': False,
            'type': 'List[_str]',
            'is_relational': False,
            'documentation': None,
        }),
        ('practiceAreas', {
            'name': 'practiceAreas',
            'is_list': True,
            'optional': False,
            'type': 'List[_str]',
            'is_relational': False,
            'documentation': None,
        }),
        ('feedbackType', {
            'name': 'feedbackType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('priority', {
            'name': 'priority',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('generatedAt', {
            'name': 'generatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('drawingAttempt', {
            'name': 'drawingAttempt',
            'is_list': False,
            'optional': True,
            'type': 'models.DrawingAttempt',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_CharacterProfile_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_CharacterProfile_fields: Dict['types.CharacterProfileKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('character', {
            'name': 'character',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('characterType', {
            'name': 'characterType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('difficultyLevel', {
            'name': 'difficultyLevel',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('strokeComplexity', {
            'name': 'strokeComplexity',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('hasLoops', {
            'name': 'hasLoops',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('hasCurves', {
            'name': 'hasCurves',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('scoreAdjustment', {
            'name': 'scoreAdjustment',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('toleranceLevel', {
            'name': 'toleranceLevel',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('averageScore', {
            'name': 'averageScore',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('successRate', {
            'name': 'successRate',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('totalAttempts', {
            'name': 'totalAttempts',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('createdAt', {
            'name': 'createdAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updatedAt', {
            'name': 'updatedAt',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_PerformanceMetric_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_PerformanceMetric_fields: Dict['types.PerformanceMetricKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('metricType', {
            'name': 'metricType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('component', {
            'name': 'component',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('value', {
            'name': 'value',
            'is_list': False,
            'optional': False,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('unit', {
            'name': 'unit',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('characterType', {
            'name': 'characterType',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('timestamp', {
            'name': 'timestamp',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)

_AdminAuditLog_relational_fields: Set[str] = set()  # pyright: ignore[reportUnusedVariable]
_AdminAuditLog_fields: Dict['types.AdminAuditLogKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('adminUserId', {
            'name': 'adminUserId',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('action', {
            'name': 'action',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('targetType', {
            'name': 'targetType',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('targetId', {
            'name': 'targetId',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('oldValue', {
            'name': 'oldValue',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('newValue', {
            'name': 'newValue',
            'is_list': False,
            'optional': True,
            'type': 'fields.Json',
            'is_relational': False,
            'documentation': None,
        }),
        ('justification', {
            'name': 'justification',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('ipAddress', {
            'name': 'ipAddress',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('userAgent', {
            'name': 'userAgent',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('timestamp', {
            'name': 'timestamp',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
model_rebuild(DrawingAttempt)
model_rebuild(FeatureVector)
model_rebuild(FeedbackRecord)
model_rebuild(CharacterProfile)
model_rebuild(PerformanceMetric)
model_rebuild(AdminAuditLog)
